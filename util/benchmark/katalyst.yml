routes:
  - pattern: "^/test/(?P<cap>.*)$"
    methods:
      - get
    downstream:
      base_url: "http://web"
      path: "/test"
  - pattern: "^/test/post$"
    methods:
      - post
    downstream:
      base_url: "http://web"
      path: "/test"
listener:
  interface: "0.0.0.0:8080"
