listener:
  interface: "0.0.0.0:8000"

hosts:
  nginx:
    balancer: round_robin
    servers:
      - "http://localhost:9999"

routes:
  - pattern: "^/$"
    methods:
      - get
    downstream:
      host: nginx
      path: "/"
